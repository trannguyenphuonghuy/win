# TODO - Thiết kế dashboard (Header + trang)

- [ ] Cập nhật `src/app/dashboard/page.tsx` để có layout dashboard có header đang hoạt động (title + card/section placeholder).
- [ ] (Tùy chọn nếu cần) Cải thiện `src/components/dashboard/Header.tsx` cho dropdown hoạt động mượt hơn (đóng dropdown còn lại khi mở dropdown mới).
- [x] Cập nhật `src/app/dashboard/page.tsx` với header + section/card placeholder.


- [ ] Chạy `npm run lint` và `npm run build` để kiểm tra lỗi (hiện tại lệnh bị chặn bởi policy PowerShell trên máy).


